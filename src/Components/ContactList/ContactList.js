import React from 'react';

const ContactList = ({ contacts }) => (
    <div>
    <ul>
            {/* {contacts.map(( {id, name} ) => (
                <li key={id}>{name}</li>
            ))} */}
    </ul>
        </div>
)
export default ContactList;